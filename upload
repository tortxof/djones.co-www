rsync -av --delete _site/ titan.djones.co:/home/tortxof/servers/djones.co/
docker run --rm \
  -u $(id -u):$(id -g) \
  -e HOME \
  -v $HOME:$HOME \
  -v $(pwd):/host \
  -w /host \
  tortxof/awscli \
  aws s3 sync _site s3://www.djones.co --delete
